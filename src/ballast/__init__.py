"""Ballast: portfolio construction and risk engine.

Notes
-----
Package root. The only thing that lives here is the version string.
It must match `version` in pyproject.toml until the build switches to
reading one from the other; bump both together on every release tag.
"""

__version__ = "0.1.0.dev0"
